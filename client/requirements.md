## Packages
framer-motion | Smooth transitions between tabs and result reveals
recharts | Visualization for confidence gauges and knowledge coverage
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely
lucide-react | Beautiful icons for the interface

## Notes
- Session ID should be generated on client load and persisted in localStorage to maintain history across refreshes.
- The UI uses a "Glassmorphism" aesthetic with a deep purple gradient background.
- Tailwind config needs to extend font families for 'Outfit' and 'Inter'.
